# Local secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Experiment outputs and large local datasets
data/
output/
outputs/
artifacts/
checkpoints/
wandb/
mlruns/
*.ckpt
*.h5
*.h5ad
*.pt
*.pth
*.zarr/

# Local paper source files; commit Markdown reading copies and source URLs instead
/docs/references/**/*.pdf

# Editors and operating systems
.idea/
.vscode/
.DS_Store
Thumbs.db

# 上游仓库浅克隆缓存（约 780 MB，只存本地）。锚定到仓库根，避免连带吃掉
# docs/references/ 下的阅读材料正文与正文引用图；那部分的排除规则见上方
# /docs/references/**/*.pdf（PDF 才是本地核验输入，不入库）。
/references/

.workbuddy
.claude/
VC-2026/
server/